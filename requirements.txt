# --- Core app ---
streamlit
python-dotenv

# --- LangChain (orchestration: chains, memory, retrievers) ---
# Pinned to the 0.3 line: the 0.3 packages below require langchain-core<1.0.
# A stray langchain-core 1.x (e.g. pulled in by langchain-docling/langgraph)
# removes langchain_core.memory and breaks `langchain` itself.
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-core>=0.3.86,<1.0
langchain-google-genai>=2.0,<3.0
langchain-text-splitters>=0.3,<0.4

# --- LLM provider ---
google-generativeai

# --- Vector store + models ---
faiss-cpu
sentence-transformers
torch

# --- Testing ---
pytest

# --- Offline index build only (parsing PDFs into the FAISS index) ---
# Not needed to run the app against the prebuilt index.
pymupdf
pdfplumber
pandas
# docling   # uncomment to rebuild the Docling index with build_docling_faiss_index.py
